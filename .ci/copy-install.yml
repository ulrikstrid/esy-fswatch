parameters:
  platform: darwin

steps:
- script: esy npm-release
  displayName: Copy the install dir for ${{ parameters.platform }}
  workingDirectory: test_dir

- publish: test_dir/_release
  artifact: install-${{ parameters.platform }}
