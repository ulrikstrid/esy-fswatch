{
  "name": "esy-fswatch-prebuilt",
  "version": "1.13.0005",
  "esy": {
    "build": "EsyPrebuiltApp.exe -action import -from '#{self.root / os}' -to '#{self.install}'",
    "exportedEnv": {
      "FSWATCH_INC": {
        "val": "-I#{self.install / 'include' / 'libfswatch' / 'c'}",
        "scope": "global"
      },
      "LIBRARY_PATH": {
        "val": "#{self.install / 'lib'}",
        "scope": "global"
      },
      "PATH": {
        "val": "#{self.install / 'bin'}#{os == 'windows' ? ';' : ':'}$PATH",
        "scope": "global"
      }
    }
  },
  "dependencies": {
    "esy-prebuilt-util": "0.1.3"
  }
}
